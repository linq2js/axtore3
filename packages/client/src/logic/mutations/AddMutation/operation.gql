mutation Add($comment: AddCommentInput, $post: AddPostInput) {
  add(comment: $comment, post: $post) {
    post {
      id
      title
      body
      userId
      createdOn
    }
    comment {
      id
      name
      email
      body
      postId
      createdOn
    }
  }
}
